@model IEnumerable<LibraryHub.Models.Author>

@{
    ViewData["Title"] = "Yazarlar";
}

<div class="page-header">
    <div>
        <h1 class="page-title">✍️ Yazarlar</h1>
        <p class="page-subtitle">Kütüphanedeki yazar kayıtlarını yönetin.</p>
    </div>

    <div class="page-actions">
        <a asp-action="Create" class="btn btn-primary">➕ Yeni Yazar</a>
    </div>
</div>

<div class="table-card">
    <table class="table table-hover align-middle">
        <thead>
            <tr>
                <th>Ad Soyad</th>
                <th class="text-end">İşlemler</th>
            </tr>
        </thead>
        <tbody>
@foreach (var item in Model) {
            <tr>
                <td class="fw-semibold">@item.FullName</td>
                <td>
                    <div class="action-group justify-content-end">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Düzenle</a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">Detay</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Sil</a>
                    </div>
                </td>
            </tr>
}
        </tbody>
    </table>
</div>
